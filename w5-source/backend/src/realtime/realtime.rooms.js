const getBoardRoom = (boardId) => `board:${boardId}`
const getWorkspaceRoom = (workspaceId) => `workspace:${workspaceId}`
const getUserRoom = (userId) => `user:${userId}`

export { getBoardRoom, getWorkspaceRoom, getUserRoom }
